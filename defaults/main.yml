---
# defaults file for common

my_common_timezone: 'Europe/Berlin'

my_suse_common_packages:
  - zsh
  - htop

my_ubuntu_common_packages:
  - git
  - curl
  - build-essential
  - htop
  - zsh
  - wget